# Domain Configuration
DOMAIN_NAME=example.com
LETSENCRYPT_EMAIL=your-email@example.com

# Traefik Dashboard Authentication (htpasswd format)
# Generate with: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$abc123$$xyz789

# DNS Provider: Cloudflare (uncomment if using Cloudflare)
CF_DNS_API_TOKEN=your-cloudflare-api-token
# CF_API_EMAIL=your-email@example.com
# CF_API_KEY=your-cloudflare-global-api-key

# DNS Provider: OVH (uncomment if using OVH)
# OVH_ENDPOINT=ovh-eu
# OVH_APPLICATION_KEY=your-ovh-application-key
# OVH_APPLICATION_SECRET=your-ovh-application-secret
# OVH_CONSUMER_KEY=your-ovh-consumer-key

# DNS Provider: Route53 (uncomment if using AWS Route53)
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_REGION=us-east-1

# DNS Provider: Google Cloud DNS (uncomment if using Google Cloud)
# GCE_PROJECT=your-gcp-project-id
# GCE_SERVICE_ACCOUNT_FILE=/run/secrets/gce_service_account

# DNS Provider: DigitalOcean (uncomment if using DigitalOcean)
# DO_AUTH_TOKEN=your-digitalocean-auth-token

# DNS Provider: Azure DNS (uncomment if using Azure)
# AZURE_CLIENT_ID=your-azure-client-id
# AZURE_CLIENT_SECRET=your-azure-client-secret
# AZURE_SUBSCRIPTION_ID=your-azure-subscription-id
# AZURE_TENANT_ID=your-azure-tenant-id
# AZURE_RESOURCE_GROUP=your-resource-group
